<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Test</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
        <a class="navbar-brand" href="#">NumGame</a>
    </nav>
    <h2>Welcome to the random number game</h2>
    <h2>Take a guess!</h2>
    <% if(info != "Correct") { %>
    <form action="/guess" method="POST">
        <div class="form-group">
            <input type="number" class="form-control" placeholder="Number" name="number">
            <small id="emailHelp" class="form-text text-muted">Number between 1 and 100.</small>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
    <% } %>
    <% if(info != null) { %>

    <% if(info === "Correct") { %>
    <p style="background-color:green; padding: 100px; font-size: 20pt;"><%= info %></p>
    <a href="/reset" class="btn btn-primary">Play again</a>
    <% } else{ %>
        <p style="background-color:red;"><%= info %></p>
    <% } %>

    <% }  else{ %>
        <p></p>
    <% } %>
    <br>
    <br>
    <br>
    <br>
    <script>
            $(document).ready(function(){
                $('#peopleBtn').click(function(){
                    // let's make the request to our OWN server!
                    $.get('/people', function(data){
                        // log the data to be sure we have it before we dive into manipulating the DOM
                        console.log("got the data", data['results']);
                    }, 'json');
                });
            });
        </script>
    <button id="peopleBtn">People</button>
</body>
</html>